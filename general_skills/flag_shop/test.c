#include <stdio.h>
#include <limits.h>

int main(int argc, char** argv){
    
    printf("INT_MAX = %d\n",INT_MAX);

    int number_flags;
    scanf("%d", &number_flags);
    printf("%d\n", number_flags);

    return 0;
}